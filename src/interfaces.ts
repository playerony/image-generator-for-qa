export interface ImageGeneratorState {
  width: number;
  height: number;
  outputSize: number;
  ratioWidth: number;
  ratioHeight: number;
}

export interface MaxCanvasArea {
  width: number;
  height: number;
}
